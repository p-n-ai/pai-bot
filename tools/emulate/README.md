# Local `emulate` Seed Config

Shared seed data for local auth and integration testing.

Use:

```bash
just emulate-auth
just emulate-google
just emulate-vercel
```

Ports:

- Vercel: `http://127.0.0.1:4000`
- Google: `http://127.0.0.1:4002`

Reference:

- `docs/local-auth-emulation.md`
