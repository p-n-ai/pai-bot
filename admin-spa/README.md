# Admin

The frontend of admin.

Previosly we're using next-js, hence why you see it named admin-spa. Moving it so we can lower memory footprint and better security.

We use [Tanstack Router](https://tanstack.com/router/latest) for the frontend router

Use `pnpm` for package and script managers. Do not run exotic deps.
