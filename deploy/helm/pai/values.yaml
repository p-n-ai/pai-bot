# SPDX-License-Identifier: Apache-2.0
# Default values for pai-bot Helm chart.

# -- Global settings
global:
  # -- Image pull secrets for private registries
  imagePullSecrets: []

# -- App (Go backend)
app:
  image:
    repository: ghcr.io/p-n-ai/pai-bot
    tag: latest
    pullPolicy: IfNotPresent
  replicas: 1
  resources:
    requests:
      cpu: 100m
      memory: 256Mi
    limits:
      memory: 512Mi
  # -- Additional environment variables (non-secret)
  env: {}
  # -- Pod annotations
  podAnnotations: {}

# -- Admin SPA
admin:
  enabled: true
  image:
    repository: ghcr.io/p-n-ai/pai-admin
    tag: latest
    pullPolicy: IfNotPresent
  replicas: 1
  resources:
    requests:
      cpu: 50m
      memory: 128Mi
    limits:
      memory: 256Mi
  podAnnotations: {}

# -- PostgreSQL
postgres:
  # -- Use built-in StatefulSet. Set to false to use an external database.
  enabled: true
  image:
    # Build and publish deploy/docker/Dockerfile.postgres at this reference.
    # The image must contain graph 1.0.0 and vector 0.8.5 for migrations to succeed.
    repository: ghcr.io/p-n-ai/pai-postgres
    tag: "pggraph-1.0.0-pgvector-0.8.5"
  storage:
    size: 10Gi
    storageClassName: ""
  resources:
    requests:
      cpu: 100m
      memory: 256Mi
    limits:
      memory: 512Mi
  # -- Credentials (only used when postgres.enabled=true)
  auth:
    database: pai
    username: pai
    # -- Password. Override in production!
    password: pai-secret

# -- Dragonfly (Redis-compatible cache)
dragonfly:
  enabled: true
  image:
    repository: docker.dragonflydb.io/dragonflydb/dragonfly
    tag: latest
  storage:
    size: 2Gi
    storageClassName: ""
  resources:
    requests:
      cpu: 50m
      memory: 128Mi
    limits:
      memory: 300Mi

# -- Configuration (non-secret env vars)
config:
  # -- Server
  serverHost: "0.0.0.0"
  serverPort: 8080
  logLevel: info
  logFormat: json
  # -- Tenant mode: "single" or "multi"
  tenantMode: single
  # -- Curriculum path inside the container
  curriculumPath: ./oss
  # -- Dev mode (disables Telegram/AI provider requirement)
  devMode: false
  # -- Feature flags
  disableMultiLanguage: false
  personalizedNudgesEnabled: true
  # -- Optional public origin for widget assets. Defaults to the ingress/request origin.
  embedBaseUrl: ""

# -- Secrets (sensitive credentials)
secrets:
  # -- Auth secret for JWT signing; also derives the AES-256-GCM key for API keys
  # stored via admin AI settings. Rotating it makes stored keys undecryptable
  # (rotate back to recover, or re-enter via the admin UI); storing keys is refused
  # while this is the default. MUST override in production.
  authSecret: change-me-in-production
  # -- Telegram bot token (required unless devMode)
  telegramBotToken: ""
  # -- AI provider keys (at least one required unless devMode)
  ai:
    openaiApiKey: ""
    codexAccessToken: ""
    codexRefreshToken: ""
    codexAccountID: ""
    anthropicApiKey: ""
    deepseekApiKey: ""
    googleApiKey: ""
    openrouterApiKey: ""
  # -- Ollama (self-hosted)
  ollama:
    enabled: false
    url: http://ollama:11434
    model: ""
  # -- AI model overrides (optional)
  aiModels:
    openai: ""
    codex: ""
    anthropic: ""
    deepseek: ""
    google: ""
    openrouter: ""
  # -- Default AI provider
  aiDefaultProvider: ""
  # -- Email/SMTP (optional)
  email:
    smtpAddr: ""
    smtpUsername: ""
    smtpPassword: ""
    fromAddress: ""
    fromName: "P&AI Bot"
    baseUrl: ""
  # -- Google OIDC (optional)
  google:
    clientId: ""
    clientSecret: ""
    allowedDomain: ""
  # -- WhatsApp (optional)
  whatsapp:
    enabled: false
    accessToken: ""
    phoneId: ""
    verifyToken: ""
  # -- Bootstrap admin
  bootstrapAdminEmail: "platform-admin@example.com"
  bootstrapAdminPassword: ""

# -- Database migration job
migration:
  # -- Run migrations as a pre-install/pre-upgrade hook
  enabled: true

# -- Ingress
ingress:
  enabled: false
  className: ""
  annotations: {}
  # -- Hostname for the ingress
  host: pai.example.com
  tls: []
  #  - secretName: pai-tls
  #    hosts:
  #      - pai.example.com

# -- Service type for the main entrypoint
service:
  type: ClusterIP
